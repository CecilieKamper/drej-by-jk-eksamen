---
import Menylayout from "../komponenter/layouts/Menylayout.astro";
import Pro01 from "../komponenter/Pro01.astro";
import Boks from "../komponenter/Boks.astro";
import Laas from "../komponenter/Laas.astro";
import Hover from "../komponenter/Hover.astro";
import Caroself from "../komponenter/Caroself.astro";
import Pil from "../komponenter/Pil.astro";
---

<Menylayout>
  <Pil href="/projekter" />
  <Pro01
    billed="/./drej-billeder/egeknude.png"
    alttext="egeknudeskål"
    h11="træet"
    h12="fra"
    h13="fyen"
  />
  <!-- hvordan ændre jeg linje skift her  -->
  <Boks sorttext="KINESERNE SAGDE NEJ, OG SÅ GIK TUREN TIL" hvidtext=" KREGME">
    <p>
      Hos min gode ven Peder Sylvest i Ore på Fyn købte jeg to ask og en rødeg
      rodende kævler. De var skåret fra, fordi kinesernes tømmeropkøber havde
      kasseret dem, og de derfor var dømt til at ende som brænde.
    </p>
    <p>
      Interessant at dansk asketræ eksporteres i store mængder til Kina. Grunden
      til, at det er rentabelt, er nok, at transporten er billig, fordi der er
      masser af tomme containere, der skal returneres til Kina, efter de er
      blevet tømt for varer i Hamburg.
    </p>
  </Boks>
  <Laas />
  <div class="grid-3 bagrund-farve">
    <Hover
      billed="/./drej-billeder/fyen-1.png"
      text="Rodende kævlerne klar til afhentning. Kævlerne øverst og nederst til venstre er ask. Nederst til højre ligger rødegen. "
    />
    <Hover
      billed="/./drej-billeder/fyen-2.png"
      text="Den store askeklods på bænken. Frihøjden er 30 cm"
    />
    <Hover
      billed="/./drej-billeder/fyen-3.png"
      text="Opdeling af stor askeklods til en rondel til et fad og én til en skål."
    />
    <Hover
      billed="/./drej-billeder/fyen-4.png"
      text="Den grovdrejede skål blev sat til tørring på øverste hylde i kontoret i 25 lag avispapir 10.01.2019. Temp. 21 grader og RF>50 %. Den 5/3 var vægten konstant, med et vægtab på 28%."
    />
    <Hover
      billed="/./drej-billeder/fyen-5.png"
      text="Her er så resultatet, en tør og og hel perfrekt grovdrejet skål uden revner."
    />
    <Hover
      billed="/./drej-billeder/fyen-6.png"
      text="Hvor stor er skålen morfar? Den er 46 cm i diametter Otto, men den mangler at få bunden drejet af."
    />
    <Hover
      billed="/./drej-billeder/fyen-7.png"
      text="Den færdig lakerede skål måler måler 46,5 x 22,5 cm. Den kunne godt være blevet 49 cm. Men da skålbakkernes max diameter er 470 mm, valgte jeg at dreje skålen ned til 46,5 cm, således at jeg kunne vende skålen, spænde den op på bænken og dreje bunden af."
    />
  </div>
  <div class="flex">
    <Caroself />
  </div>

  <div class="obagrund border-2"></div>

  <style>
    .flex {
      display: flex;
      justify-content: center;
      border-bottom: 2px solid var(--mørke-orange);
    }
    .bagrund-farve {
      display: none;
      border-bottom: 2px solid var(--mørke-orange);
    }

    .obagrund {
      background-color: #e18658;
    }

    p {
      font-family: Arimo, sans-serif;
      font-size: 1rem;
      font-style: normal;
      font-weight: 400;
      line-height: 1.5rem;
    }
    @media only screen and (min-width: 800px) {
      .grid-3 {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        justify-items: center;
        padding: 30px 20px 30px 20px;
        gap: 10px;
      }
      .flex {
        display: none;
        border-bottom: 3px solid var(--mørke-orange);
      }
      .bagrund-farve {
        background-color: #e18658;
        border-bottom: 3px solid var(--mørke-orange);
      }
    }
  </style>
  <script>
    // script.js

    document.addEventListener("DOMContentLoaded", function () {
      const carousel = document.querySelector(".carousel");
      const carouselInner = carousel.querySelector(".carousel-inner");
      const items = carouselInner.querySelectorAll(".carousel-item");
      const nextButton = carousel.querySelector(".carousel-control.next");
      const prevButton = carousel.querySelector(".carousel-control.prev");

      let currentIndex = 0;
      const totalItems = items.length;

      function showSlide(index) {
        items.forEach((item, i) => {
          item.classList.toggle("active", i === index);
        });
      }

      function nextSlide() {
        currentIndex = (currentIndex + 1) % totalItems;
        showSlide(currentIndex);
      }

      function prevSlide() {
        currentIndex = (currentIndex - 1 + totalItems) % totalItems;
        showSlide(currentIndex);
      }

      nextButton.addEventListener("click", nextSlide);
      prevButton.addEventListener("click", prevSlide);

      // Optional: Auto-play functionality
      // setInterval(nextSlide, 3000);
    });
  </script>
</Menylayout>
